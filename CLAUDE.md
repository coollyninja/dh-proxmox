# dh-proxmox agent context

Read `../CLAUDE.md` before changing this repository.

This is a public, read-only Deckhand plugin. Preserve the fixed-path API mapping, logical target
aliases, file-based credentials, TLS verification, redirect refusal, typed sanitized errors, and
full adapter lifecycle. Do not add real endpoints, node names, VM IDs, secrets, or site policy.

Run Ruff, Ruff format check, strict mypy, pytest, and the public-surface scanner before publishing.
